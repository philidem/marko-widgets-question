# Marko Widget Question #1

```sh
$ npm install
$ node src/server.js
```

Two identical widgets (`good-widget` and `bad-widget`) with the only difference in widget file name.

`good-widget`: `index.js`
`bad-widget`: `widget.js`
